# Copy to .env.local and fill in.
# When unset, the app runs fully on localStorage. Supabase auth + cross-device sync
# activates only when both vars are present.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
